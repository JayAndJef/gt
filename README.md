# gt

Never lose your important directories again.

```
❯ cargo run -- --help
    Finished dev [unoptimized + debuginfo] target(s) in 0.24s
     Running `target/debug/gt --help`
Usage: gt <command> [<args>]

Bookmark and access directories

Options:
  --help            display usage information

Commands:
  add               Add a bookmark to a directory
  get               Retrieve the path of a bookmark
  delete            Delete a bookmark
  list              list all bookmarks
```
